============
Installation
============

At the command line::

    $ easy_install python_lib

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv python_lib
    $ pip install python_lib
